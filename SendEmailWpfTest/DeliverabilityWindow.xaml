<Window x:Class="SendEmailWpfTest.DeliverabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Email Deliverability Diagnostics" 
        Height="700" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#2D2D30"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#858585"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#16825D"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#E81123"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0E639C"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#1177BB"/>
        <SolidColorBrush x:Key="InputBrush" Color="#3F3F46"/>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Outer border for shadow effect -->
    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Direction="270" 
                            ShadowDepth="0" 
                            BlurRadius="20" 
                            Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="50">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="20,0,0,0">
                        <TextBlock Text="&#xE9D9;" 
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="18"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,10,0"/>
                        <TextBlock Text="Email Deliverability Diagnostics" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Grid.Column="1"
                            x:Name="btnCloseWindow"
                            Width="46"
                            Height="50"
                            Click="CloseWindow_Click">
                        <TextBlock Text="&#xE711;" 
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="0,8,0,0">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="30">
                <StackPanel>
                    <!-- Domain Info -->
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="20"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Checking Domain"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,10"/>
                            <TextBlock x:Name="txtDomain"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Check Button -->
                    <Button x:Name="btnCheckDeliverability"
                            Content="Run Diagnostics"
                            Style="{StaticResource ButtonStyle}"
                            Click="CheckDeliverability_Click"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,20"
                            MinWidth="160"/>

                    <!-- Results Panel -->
                    <StackPanel x:Name="pnlResults" Visibility="Collapsed">
                        <!-- Results will be added here dynamically -->
                    </StackPanel>

                    <!-- Recommendations -->
                    <Border x:Name="pnlRecommendations"
                            Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="20"
                            Visibility="Collapsed"
                            Margin="0,20,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Header with Copy Button -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="&#xE946;" 
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Recommendations for SmarterASP.NET"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <Button x:Name="btnCopyRecommendations"
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Content="&#xE8C8;"
                                    FontFamily="Segoe MDL2 Assets"
                                    Width="32"
                                    Height="32"
                                    FontSize="14"
                                    ToolTip="Copy recommendations to clipboard"
                                    Click="CopyRecommendations_Click"
                                    Cursor="Hand"
                                    Margin="10,0,0,0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="4">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource InputBrush}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Recommendations Content -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                                <TextBlock TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           Margin="0,0,0,10">
                                    <Run Text="1. Contact SmarterASP.NET Support:"/>
                                    <LineBreak/>
                                    <Run Text="   Ask them to configure SPF, DKIM, and DMARC records for your domain."/>
                                </TextBlock>

                                <TextBlock TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           Margin="0,0,0,10">
                                    <Run Text="2. Microsoft-Specific Whitelisting:"/>
                                    <LineBreak/>
                                    <Run Text="   - Sign up for Microsoft SNDS (Smart Network Data Services)"/>
                                    <LineBreak/>
                                    <Run Text="   - URL: https://postmaster.live.com/snds/"/>
                                    <LineBreak/>
                                    <Run Text="   - This helps monitor your IP reputation with Microsoft"/>
                                </TextBlock>

                                <TextBlock TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           Margin="0,0,0,10">
                                    <Run Text="3. Junk Mail Reporting (JMR):"/>
                                    <LineBreak/>
                                    <Run Text="   - Sign up at: https://postmaster.live.com/"/>
                                    <LineBreak/>
                                    <Run Text="   - This provides feedback on spam complaints"/>
                                </TextBlock>

                                <TextBlock TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}">
                                    <Run Text="4. Email Best Practices:"/>
                                    <LineBreak/>
                                    <Run Text="   - Avoid spam trigger words in subject lines"/>
                                    <LineBreak/>
                                    <Run Text="   - Include an unsubscribe link if sending bulk emails"/>
                                    <LineBreak/>
                                    <Run Text="   - Use a consistent From address"/>
                                    <LineBreak/>
                                    <Run Text="   - Ensure your email has both HTML and plain text versions"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Action Buttons -->
            <Border Grid.Row="2"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8"
                    Padding="30,20">
                <Button Content="Close" 
                        Style="{StaticResource ButtonStyle}"
                        Background="#3F3F46"
                        Click="CloseWindow_Click"
                        HorizontalAlignment="Right"
                        MinWidth="100"/>
            </Border>
        </Grid>
    </Border>
</Window>
