{
  // SMTP Server Configuration
  "SmtpSettings": {
    // SMTP server hostname (e.g., smtp.gmail.com, mail.example.com)
    "Host": "mail5018.site4now.net",
    
    // SMTP server port
    // Common ports: 465 (SSL/TLS), 587 (STARTTLS), 25 (unencrypted - not recommended)
    "Port": 465,
    
    // SMTP authentication username (usually your email address)
    "Username": "support@nvadi.com",
    
    // SMTP authentication password
    // WARNING: Do not commit passwords to source control!
    // Leave empty and set via User Secrets, Environment Variables, or secure configuration
    "Password": ""
  },

  // Default Email Settings
  "EmailDefaults": {
    // Default sender email address
    "FromAddress": "support@nvadi.com",
    
    // Default sender display name (shown in email clients)
    "FromName": "nv�di Support"
  }
}
